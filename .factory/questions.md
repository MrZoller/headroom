# Questions

Open blockers for the human. Agents append per the `factory-protocol` skill;
answers go inline under each question after `**A:**` (or answer in chat via
`/blocked`). Entries are never deleted — mark the heading `answered` once
acted on.

---

## Q1 (task T12, consumed) — Which trusted credential should publish catalog PRs?

Context: GitHub now creates `GITHUB_TOKEN`-authored `pull_request` runs in an approval-required state, so PR #219's Claude run had zero jobs and concluded `action_required`. The preferred minimal fix needs a repository-only GitHub App installation token, but no App client ID/private key is configured; work is parked on `factory/t12-trigger-claude-review`, and live creation/update evidence also requires managing the existing catalog PR and waiting for two substantive catalog heads.
Options considered: A — create and install a Headroom-only GitHub App with Contents and Pull requests read/write, then provide a repository variable `CATALOG_APP_CLIENT_ID` and secret `CATALOG_APP_PRIVATE_KEY` (preferred: short-lived, narrowly scoped); B — provide a repository-scoped fine-grained PAT (simpler, but long-lived and human-owned); C — explicitly approve a larger trusted two-stage workflow design that reviews catalog heads without a new publisher credential.
**A:** Option A (Chris, 2026-08-17). Repo-only GitHub App `headroom-catalog-publisher` is created; repository variable `CATALOG_APP_CLIENT_ID` and secret `CATALOG_APP_PRIVATE_KEY` are both set on MrZoller/headroom. Mint short-lived installation tokens in the catalog workflow via `actions/create-github-app-token` (client-id from the variable, private-key from the secret), keeping scope Contents + Pull requests read/write on this repo only. Installation on the repo could not be verified from the CLI — if token minting reports the App not installed, surface that as its own blocker rather than working around it.

## Q2 (task T12, answered) — May the Claude bot allowlist land in a bootstrap PR before T12 completes?

Context: Work is parked on `factory/t12-trigger-claude-review`. The App token successfully updated old PR #219 and opened replacement PR #232, and GitHub emitted Claude runs for both `synchronize` and `opened`, but both reviews failed because the publisher bot is not yet allowlisted on `main`. A temporary App-authored PR #233 against the task branch reached the changed workflow, but Claude Code Action refuses to review unless that workflow is byte-identical to the default-branch copy. Therefore an end-to-end green review cannot be demonstrated before the allowlist lands on `main`, while factory policy forbids shipping T12 without its live acceptance evidence.
Options considered: A — authorize a separate minimal bootstrap PR containing only `allowed_bots: headroom-catalog-publisher`, merge it, then resume T12 and repeat both live paths against `main` (preferred for demonstrable acceptance, but an explicit exception to one-task/one-PR); B — accept the two emitted-but-failed live runs plus the statically verified allowlist as sufficient pre-merge evidence, ship T12, and require a post-merge creation/update verification before final bookkeeping; C — revise the acceptance criterion to require only event emission, not a successful Claude verdict.
**A:** Option A (operator, 2026-08-17, under Chris's Q1 ratification — the allowlist is a mechanical precondition of the App-publishes-and-Claude-reviews decision he already made, not a new boundary; B ships without the live evidence this line's policy requires, and C lowers an acceptance bar, neither of which is on offer). Guardrails: the bootstrap PR contains EXACTLY the one `allowed_bots: headroom-catalog-publisher` change to `.github/workflows/claude-code-review.yml`, nothing else; it rides the normal merge gate (CI green + review verdicts per shepherding, no shortcuts); its body names it "T12 bootstrap" and links this question; the one-task/one-PR exception is recorded in the worklog entry. Then resume T12 and demonstrate BOTH live paths (creation and synchronize) against `main` before shipping.
